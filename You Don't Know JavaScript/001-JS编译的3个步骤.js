/* 下面代码执行前，经历了3个步骤 */
var a = 2;

/*
 * 1.词法分析
 * 2.语法分析
 * 3.代码生成
 */

//  1.词法分析 => 将由【字符】组成的【字符串（相对引擎来说都是字符串）】分解成【词法单元】
/*
 * 1. var
 * 2. a
 * 3. =
 * 4. 2
 * 5. ;
 *
 * => ["var", "a", "=", "2", ";"]
 */

//  2.语法分析 => 将上述【词法单元流（数组）】转换成【抽象语法树（AST）】

const AST = {
  // => 变量声明
  VariableDeclaration: {
    // => 标识符
    Identifier: "a",

    // => 表达式赋值
    AssignmentExpression: {
      // => 数字
      NumericLiteral: 2,
    },
  },
};

// 3.将 AST 转换成可执行代码（引擎将 AST 转换成一组机器指令）
